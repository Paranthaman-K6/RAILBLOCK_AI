# Render backend-only: this file is for local/docker single-image fallback (same-origin).
# Vercel frontend MUST set VITE_API_URL via dashboard (Project → Settings → Environment Variables)
# → VITE_API_URL=https://railblock-ai-up0g.onrender.com for Production/Preview/Development
# api.ts prod fallback will use Render URL if this stays empty, but dashboard var is canonical.
VITE_API_URL=""
